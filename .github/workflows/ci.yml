name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  configured-tests:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Run configured tests
        run: ./scripts/test.sh
