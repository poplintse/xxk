# 发布质量状态

## 已自动验证

- Debug 构建与 `.app` 启动。
- SwiftData 内存库往返、临时磁盘关闭后重开、完整行程字段和级联删除。
- 15分钟吸附、时间块拉伸、跨午夜显示。
- 多晚住宿的默认入住和退房时间、手工跨度保留及再次拖动。
- 跨天住宿每日片段。
- 非零时间轴起点的跨午夜拖放落点。
- 旅行时区显示、混合币种分组、金额溢出保护及无效设置修复。
- 时间冲突检测、1000个非重叠事项和10000个高度重叠事项扫描。
- 共21项自动化测试，覆盖4个测试套件。
- Apple Silicon Release 构建（arm64）。
- App Sandbox 与 Hardened Runtime。
- Asset Catalog、应用图标和 Info.plist。
- ad-hoc 签名、严格签名校验和 zip 完整性。
- 公证脚本在联网提交前验证 Developer ID 签名与归档校验和，并已确认会拒绝当前 ad-hoc 候选包。
- 发布验证脚本从 ZIP 中解包实际交付应用，自动核对包结构、版本、构建号、最低系统版本、arm64 架构、Hardened Runtime、App Sandbox、严格签名与归档内容。
- Apple Silicon 测试版已生成版本化 ZIP、独立 SHA-256 和测试者说明；从最终测试 ZIP 解包的 `0.1.0 (1)` 应用已实际启动，并在验收后正常关闭。
- Release 进程启动、主窗口可见和沙盒数据库创建。

## 已完成的 macOS 图形界面验收

- 新建旅行，并在旅行编辑器中保存名称和目的地；退出并重新启动后数据仍然存在。
- 新建普通游玩事项和2晚住宿事项；规划视图正确生成连续3天的住宿片段。
- 月览视图在入住、续住和退房3天均显示住宿，并正确显示“日、一、二、三、四、五、六”七个星期标题。
- 通过真实指针拖放，将待安排事项放入第1天 10:00；待安排数量由1变为0，事项显示为 10:00–12:00。
- 对时间块执行两次15分钟增量调整，结束时间变为12:30；再通过“移回待安排”操作恢复到待安排列表。
- 再次通过真实指针拖放，将“夜游验收”安排为 23:15–次日01:15，直接验证跨午夜落点与显示。
- 修正时间块拖动和边缘手柄的手势竞争后，通过真实指针分别拉伸下边缘与上边缘，最终时间由 23:15–次日01:15 调整为 22:45–次日02:15；重启后中间结果仍然存在。
- 针对日期列内部系统拖放无法命中相邻落点的问题，将已安排时间块改为按日期差与分钟差直接移动；真实指针把事项从 8月17日21:45–次日01:15 移到 8月18日20:45–次日00:15，持续时间保持3.5小时。
- 跨列移动后再次用真实指针拉伸下边缘和上边缘，最终时间变为 8月18日20:15–次日00:45，移动与拉伸没有互相误触。
- 行程表显示时间、事项、路线、内容、费用和重点事项6列；住宿编辑器显示晚数、默认住宿时间和手工起止时间状态。
- 设置窗口验证了默认住宿时间 23:00–15:00、时间轴设置和默认币种 CNY。
- 验证“新建旅行”与“新建事项”菜单快捷键，并在约 980 点窗口宽度下确认三个主视图及规划分栏仍可使用。
- 关键图标按钮已提供显式辅助功能标签；时间块暴露时间说明、增减和自定义辅助功能操作。
- 临时切换 macOS 系统深色外观并仅捕获应用窗口完成视觉巡检；文字、选中态、分隔线和时间网格均清晰，验收后已恢复原系统外观。
- 通过事项编辑器选择游玩参观、住宿、交通、餐饮、购物和其他全部类型；长交通标题完整保存，真实键盘输入的 ¥1,234,567,890.12 大额费用重开后仍存在，空重点事项保持为空。
- 创建1晚住宿并验证原默认时间 23:00–15:00；把设置临时改为22:00–14:00后，新建住宿立即使用新时间，随后关闭默认开关并手工覆盖为14:15退房；验收完成后设置已恢复为23:00–15:00。
- 创建两个时间重叠的住宿，所有跨日片段均暴露“与其他事项时间重叠”提示；不重叠事项保持普通编辑提示。
- 月览8月16日与17日均显示两个住宿事项；执行“在规划视图中打开”后自动切回规划并锚定周日8/16。
- 详细行程表显示3行、6列；在事项详情检查器中修改路线后，表格路线列和规划事项编辑器均显示同一新值。
- 从侧栏删除仅用于验收的“未命名旅行”后，侧栏旅行数据行消失，详情区返回“开始规划旅行”，其全部测试事项随旅行清理。

## 发布前仍需外部条件

- 安装有效的 Developer ID Application 证书。
- 使用真实证书重新签名。
- 配置 `notarytool` profile，提交公证并装订票据。
- 在 macOS 14 与当前 macOS 版本上执行完整人工交互清单。
- 使用 VoiceOver 实际听读关键控件。

## 人工交互清单

1. 使用 VoiceOver 实际听读。
2. 在 Apple Silicon Mac 的 macOS 14 上执行冒烟测试。
